name: Never Run This

on: workflow_dispatch

jobs:
  Run:
    runs-on: ubuntu-latest
    steps:
      - name: 1st step
        run: echo yeahh!
      - name: 2nd step
        run: |
          pwd
          ls -al
      - name: 3rd step create a file
        run: |
          echo "Hello World" > hello.txt
          cat hello.txt
      - name: 4th step
        run: |
          pwd
          ls -al
