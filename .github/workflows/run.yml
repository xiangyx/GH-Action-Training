name: Never Run This

on: workflow_dispatch

jobs:
  Run:
    runs-on: ubuntu-latest
    steps:
      - name: 1st step
        run: echo yeahh!
      - name: 2nd step
        run: |
          pwd
          ll
